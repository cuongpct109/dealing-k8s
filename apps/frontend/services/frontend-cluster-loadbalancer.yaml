apiVersion: v1
kind: Service
metadata:
  name: frontend-cluster-loadbalancer
  namespace: frontend
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: frontend
  type: LoadBalancer
status:
  loadBalancer: {}
