apiVersion: v1
kind: Service
metadata:
  name: minio-cluster-loadbalancer
  namespace: database
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9000
  selector:
    app: minio
  type: LoadBalancer
status:
  loadBalancer:
    ingress:
      - ip: 172.30.0.104
